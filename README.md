
# MNIST Web App

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the app: `python app.py`
3. Open in browser: `http://127.0.0.1:5000/`

## Notes
- Replace `model/model.pkl` with your trained model params (pickle file).
- Includes speech output, confidence display, and chime.
